name: build/deploy python resources

on: 
  push:
    branches: ["main"]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout Code
      uses: actions/checkout@v4
    
    - name: Login
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build Worker
      uses: docker/build-push-action@v5
      with:
        context: ./Core/backend
        push: true
        tags: ghcr.io/rprima/infrastructure-playground/worker:latest

    - name: Build CLI
      uses: docker/build-push-action@v5
      with:
        context: ./Core/frontend
        push: true
        tags: ghcr.io/rprima/infrastructure-playground/cli:latest